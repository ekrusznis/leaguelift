import { AuditHistoryPage } from "../auditHistory/AuditHistoryPage";

export function PlatformAuditPage() {
	return (
		<AuditHistoryPage
			title="Platform Audit History"
			description="Immutable activity across Rally26. Platform administrators can filter across organizations, teams, users, actions, results, and dates."
		/>
	);
}
